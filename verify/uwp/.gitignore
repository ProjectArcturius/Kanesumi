# 构建产物
bin/
obj/
AppPackages/

# 图标入库。
#
# 早先忽略它们，理由是「图形可复现、提交二进制让 diff 不可读」—— 但那是错的：
# Package.appxmanifest **引用**这些文件，忽略掉会导致干净克隆后构建失败。
# 验证工程的价值在于开箱可构建，几 KB 的 PNG 不是负担。
# tools/make-icons 保留作重新生成用（改配色 / 加尺寸时跑）。

# Visual Studio / Rider
.vs/
.idea/
*.user
*.suo

# 打包中间件
*.appxupload
*.appxsym
