[package]
name = "kanesumi-gallery"
description = "Kanesumi（矩隅）· Gallery 应用 —— 三层测试阶梯的 daily driver（参 Ether-main PLAN.md §4.4）"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
authors = ["Takahashi Rinta <mc1586182829@outlook.com>"]

[dependencies]
kanesumi-core = { path = "../kanesumi-core" }
kanesumi-canvas = { path = "../kanesumi-canvas" }
kanesumi-anim = { path = "../kanesumi-anim" }
kanesumi-structure = { path = "../kanesumi-structure" }
kanesumi-controls = { path = "../kanesumi-controls" }

# daily driver 接 harness 外壳（Linux 上 platform::run）。Phase 3 续（HANDOVER §1）。
kanesumi-harness = { path = "../kanesumi-harness" }
