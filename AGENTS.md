# AGENTS.md —— Kanesumi 工作流守则

本文件记录在本仓协作时的**硬性规则**。违反视为返工，会被要求重做。

## 一、提交规范（2026-09-22 立）

### 1.1 标题：Conventional Commits 前缀

| 前缀 | 用于 |
|---|---|
| `feat` | 新增能力（新控件、新 API、新机制）。 |
| `fix` | 修复缺陷（含回归修复）。 |
| `docs` | 只改文档 / 注释。 |
| `refactor` | 不改外部行为的结构调整。 |
| `perf` | 性能。 |
| `test` | 只增改测试。 |
| `build` | 构建 / 依赖 / manifest。 |
| `chore` | 杂项（脚本、忽略规则、版本号）。 |
| `style` | 纯格式化（rustfmt 等）。 |

作用域写模块名：`fix(tab_row): …`、`feat(canvas/text): …`、`docs(audit): …`。

### 1.2 正文：中文，句号结尾

- **正文用中文。**
- **每一句都以句号（。）结尾**，含最后一句。
- 写清**为什么**，不只写改了什么；引用设计文档用 `参 <doc> §<section>`。

### 1.3 粒度：一个功能块一次 commit

- 一次提交只做**一件事**。做法不同、可独立回滚的改动必须分开提交。
  - 正例：`fix(tab_row): 空表渲染不再越界。` 与 `fix(repeater): columns=0 不再除零。` 分成两次。
  - 反例：把「溢出契约 + 容器裁剪 + 消除 panic + 鲁棒性」压进一个提交（2026-09-22 已发生一次，`47e480f`）。
- 判据：**这个提交若被回滚，会不会连带回滚掉不该回滚的东西？** 会，就拆开。
- 重构与行为变更分开；纯格式化单独一次；文档与代码分开（除非文档就是该改动的规格说明）。

### 1.4 推送：未经指示不得推送

- **未经明确指示，不得 `git push`。** 提交完成后停下等指示。
- 同理不得 force-push，不得改写已推送的历史。

### 1.5 提交前自检

- `cargo test --workspace` 全绿。
- `cargo clippy --workspace --all-targets` 无**新增**告警。
- 行为变更（尤其默认值变化）必须带回归测试，并在相关文档留痕。

## 二、代码与文档约定（摘）

- 注释用中文 Vintage Words 风格，交叉引用写 `参 <doc> §<chapter>`。
- 设计语言称「Kanesumi 风格 / Kanesumi 铁律 / Kanesumi 控件库」；`Metro*` 标识符（`MetroText`/`MetroTheme`…）是代码名，保留不重命名；缓动族称「UWP 缓动」（`UwpEasing`）。
- 新增 / 修改控件前先读 `docs/CONTROL_SPEC.md` 对应节；该节缺失时**先补规格再写实现**。规格来源分级见 `docs/PORT_ROADMAP.md` §Ⅰ（开源 / 闭源 / 实证）。
- 溢出与容器语义按 `docs/COMPOSITION.md` 的强制契约执行：文本必须显式声明换行与越界策略，容器必须裁剪子内容。
- 诊断落盘写持久路径（`~/.local/state/`），不得只写 tmpfs `/tmp`。
- 审计与整改结论写进 `docs/`，不要只留在会话里；`docs/MATURITY_AUDIT_2026-09-22.md` 是当前成熟度整改的入口。
